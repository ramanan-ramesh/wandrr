import 'package:flutter/material.dart';

class PlanDataList extends StatelessWidget {
  const PlanDataList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Padding(
        padding: EdgeInsets.all(16.0),
        child: Text('PlanDataList: Displays a list of plan data.'),
      ),
    );
  }
}
