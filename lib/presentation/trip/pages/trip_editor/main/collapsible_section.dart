import 'package:flutter/material.dart';

class CollapsibleSection {
  final String title;
  final IconData icon;
  final Widget child;

  CollapsibleSection({
    required this.title,
    required this.icon,
    required this.child,
  });
}
